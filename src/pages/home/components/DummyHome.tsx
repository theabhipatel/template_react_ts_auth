/** ########################################################
 *   This is the dummy component to show Directory structure
 *   How we should put component for pages in their own Dir.
 *  ######################################################## */

const DummyHome = () => {
  return <div>{/** Add component code here */}</div>;
};

export default DummyHome;
